String.prototype.yell = function () {
    return `OMG!!! ${this.toUpperCase()}!!!!!`
}

// assigns .yell the uppercase method and adds the string

Array.prototype.pop = function () {
    return 'SORRY I WANT THAT ELEMENT, I WILL NEVER POP IT OFF!'
}

// overrides .pop method to just produce a string and no longer remove and element from an array

